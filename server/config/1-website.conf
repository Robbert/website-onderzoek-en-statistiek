server {
    listen              443 ssl http2;
    server_name         www.onderzoek.amsterdam.nl www.statistiek.amsterdam.nl;
    return 301          https://onderzoek.amsterdam.nl$request_uri;
}

proxy_cache_path /var/cache/nginx/prod levels=1:2 keys_zone=cache_api_prod:10m inactive=7d use_temp_path=off;

server {
    listen              443 ssl http2 default_server;
    server_name         onderzoek.amsterdam.nl statistiek.amsterdam.nl;

    client_max_body_size 256M;

    ssl_certificate /etc/ssl/os-certs/onderzoek_amsterdam_nl.crt;
    ssl_certificate_key /etc/ssl/os-certs/server.key;

    # temporarily redirect to www.amsterdam.nl/onderzoek/
    location ~ ^/(mrIWeb|spssmr|SPSSMR|ois|)/ {
        rewrite ^/(.*)$ https://www.amsterdam.nl/onderzoek/link-niet-beschikbaar permanent;
    }

    location /extern/ {
        proxy_pass https://os-amsterdam.gitlab.io/;
    }

    # serve static files
    location ~ ^\/static\/([^\/]+)?.* {
        add_header 'Access-Control-Allow-Origin' '*';
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
        add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
        add_header 'Access-Control-Max-Age' 1728000;
        etag off;
        add_header Cache-Control "public,max-age=604800,immutable";
        root  /usr/share/nginx/html/prod;
        # redirect requests that don't match any file to root of application
        # this enables client side routers
        try_files $uri $uri/ /static/$1/index.html /$1;
    }

    location /api/search-content {
        proxy_cache cache_api_prod;
        proxy_ignore_headers Cache-Control;

        # update every 5 minutes and always sends a cached version
        proxy_cache_valid 200 5m; 
        proxy_connect_timeout 1ms; 
        proxy_cache_revalidate on;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_background_update on;
        proxy_cache_lock on;
        
        proxy_pass http://os-website-prod:3000/api/search-content;

        add_header X-Cache-Status $upstream_cache_status;
         # force browser to refresh after 10 minutes
        add_header Cache-Control "public,max-age=600, stale-while-revalidate=120";
    }

    location / {
        proxy_pass http://os-website-prod:3000; 
    }

}

proxy_cache_path /var/cache/nginx/acc levels=1:2 keys_zone=cache_api_acc:10m inactive=7d use_temp_path=off;

server {
    listen              443 ssl http2;
    server_name         acc.onderzoek.amsterdam.nl;

    client_max_body_size 256M;

    ssl_certificate /etc/ssl/os-certs/onderzoek_amsterdam_nl.crt;
    ssl_certificate_key /etc/ssl/os-certs/server.key;

    # serve static files
    location ~ ^\/static\/([^\/]+)?.* {
        auth_basic "Acceptatie omgeving";
        auth_basic_user_file /etc/nginx/.htpasswd;
        add_header 'Access-Control-Allow-Origin' '*';
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
        add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';
        add_header 'Access-Control-Max-Age' 1728000;
        etag off;
        add_header Cache-Control "public,max-age=604800,immutable";
        root  /usr/share/nginx/html/acc;
        # redirect requests that don't match any file to root of application
        # this enables client side routers
        try_files $uri $uri/ /static/$1/index.html /$1;
    }

    location /api/search-content {
        proxy_cache cache_api_acc;
        proxy_ignore_headers Cache-Control;

        # update every 5 minutes and always sends a cached version
        proxy_cache_valid 200 5m; 
        proxy_connect_timeout 1ms; 
        proxy_cache_revalidate on;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_background_update on;
        proxy_cache_lock on;
        
        proxy_pass http://os-website-acc:3000/api/search-content;

        add_header X-Cache-Status $upstream_cache_status;
         # force browser to refresh after 10 minutes
        add_header Cache-Control "public,max-age=600, stale-while-revalidate=120";
    }

    location / {
        auth_basic "Acceptatie omgeving";
        auth_basic_user_file /etc/nginx/.htpasswd;
        proxy_pass http://os-website-acc:3000;
    }
}